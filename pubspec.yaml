name: vote
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^0.8.4+4
  path_provider: ^2.0.5  # Keep the latest version
  shared_preferences: ^2.0.6  # Keep the latest version
  sqflite: ^2.0.0+4
  path: ^1.8.0
  cupertino_icons: ^1.0.6
  postgres: ^2.4.1
  flutter_dotenv: ^5.0.2
  camera: ^0.10.0+3
  http: ^0.13.4
  image: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env
