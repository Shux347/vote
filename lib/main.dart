import 'package:flutter/material.dart';
import 'package:vote/login.dart';

void main() {
  runApp(
    const MaterialApp(
      home: Scaffold(
        body: LoginPage(),//LoginSection
      )
    )
  );
}